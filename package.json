{
  "name": "cex-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Production-grade Centralized Exchange (CEX) Monorepo",
  "scripts": {
    "prebuild": "pnpm --filter @cex/backend prisma:generate",
    "build": "pnpm -r run build",
    "predev": "pnpm --filter @cex/backend prisma:generate",
    "dev": "pnpm -r run dev",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "clean": "pnpm -r exec rm -rf node_modules dist .turbo"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.5.0",
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
